# vim: filetype=sh
eval "$(lorri direnv)"

export PATH=/home/robin/.cargo/bin:$PATH:./target/debug/

# mkdir -p .vim

# RA_PATH=$(readlink -e $(which rust-analyzer))

# echo "{ \"rust-analyzer.enable\": true,\
#     \"rust-analyzer.server.path\": \"$RA_PATH\" }" | jq > .vim/coc-settings.json



#     # \"rust-analyzer.checkOnSave.extraArgs\":\
#     # [\"--target-dir", "/tmp/rust-analyzer-check\" ]\
